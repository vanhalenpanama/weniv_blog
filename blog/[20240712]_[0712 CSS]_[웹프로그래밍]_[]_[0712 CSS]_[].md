```python

프론트엔드에서 확인해볼 사항

크로스브라우징
        크롬과 엣지가 같은 화면으로 보이는지
      	모바일과 데스크톱이 같은 ui로 보이는지

시멘틱한 마크업
	마크업이 적재적소에 들어가있는지
	header안에 h1이 있는지
	section안에 h2부터 있는지

웹 접근성
	스크린 리더로 읽히는 것이 잘 읽히는 지
	키를 조작했을 때 제대로 먹히느니
스타일

..................................................................

# thead, tbody, tfoot

- 테이블의 머리글, 본문, 바닥글을 의미.
- 구역을 나누는 요소로 사용.
- 선택적으로 사용하면 됩니다. 필수 요소는 아닙니다. 코드의 가독성을 위해 명시적으로 사용하면 좋습니다
- <thead>: 테이블 행 블록(row block) 내에 제목 열 그룹(column headers)으로 구성할 경우 사용.
- <tbody>: 행 블록 내에 테이블 데이터로 구성할 때 사용.
- <tfoot>: 행 블록 내에 열 요약(column summaries)로 구성할 때 사용.


thead태그가 없으면 데이터를 쏴줘도 받지 못한다


colspan: 열 병합,  rowspan: 행 병합

..................................................................

CSS 구조

셀렉터(선택자)
프로퍼티
벨류

p태그가 셀렉터 color가 프로퍼티 red 벨류

p {
	color:red;
}




CSS 적용하는 방법

1.인라인 방식 - 태그 자체에 style 속성으로 스타일을 주는 방식입니다.
2.내부 스타일 - head 태그 안 style 태그를 사용하여 스타일을 주는 방식입니다.
3.외부 스타일 <link rel="stylesheet" href="style.css">




..................................................................


아이디 선택자 
- #으로 시작
- 문서 내에서 유일한 요소를 선택할 때 사용
- 선택자 이름은 대소문자를 구분


/* 자손 */
section p {
    color: red;
}
/* 직계 자식 */
section > p {
    color: red;
}





```